<div id="entity-header">
  <h2>{{person.name}}</h2>
</div>
{% if current_roles %}
<h4>{{person.name}} is currently involved with</h4>
{% for role in current_roles %}
<div>
  <h5><a href="{{role.show.slug}}">{{role.show.name}}</a> - {{role.name}}</h5>
  <p>{% for performance in role.show %}
    {{performance.time|date:"g:iA"|lower}}, {{performance.start_date|date:"D dS F Y"}} - {{performance.end_date|date:"D dS F Y"}} at <a href="/venues/{{performance.venue.slug}}">{{performance.venue.name}}</a>
    {% endfor %}</p>
  <p>{{role.show.desc}}</p></div>
{% endfor %}

<h4>{{person.name}} is preparing for</h4>
<ul class="large-block-grid-2">
  {% for role in future_roles %}
  <li>
    <h5><a href="{{role.show.slug}}">{{role.show.name}}</a> - {{role.name}}</h5>
    <p>{% for performance in role.show %}
      {{performance.time|date:"g:iA"|lower}}, {{performance.start_date|date:"D dS F Y"}} - {{performance.end_date|date:"D dS F Y"}} at <a href="/venues/{{performance.venue.slug}}">{{performance.venue.name}}</a>
      {% endfor %}</p>
    <p>{{role.show.desc}}</p></li>
  {% endfor %}
</ul>
{% for role in past_roles %}
<div>
  <h5><a href="{{role.show.slug}}">{{role.show.name}}</a> - {{role.name}}</h5>
  <p>{% for performance in role.show %}
    {{performance.time|date:"g:iA"|lower}}, {{performance.start_date|date:"D dS F Y"}} - {{performance.end_date|date:"D dS F Y"}} at <a href="/venues/{{performance.venue.slug}}">{{performance.venue.name}}</a>
    {% endfor %}</p>
  <p>{{role.show.desc}}</p></div>
{% endfor %}
